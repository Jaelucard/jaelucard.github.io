# Copy to .env and fill in. .env is gitignored.
# Used when user_facts.llm.provider is "anthropic".
ANTHROPIC_API_KEY=
# Used when user_facts.llm.provider is "ollama". POSTs only to http://localhost:11434/api/generate.
OLLAMA_MODEL=
